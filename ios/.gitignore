# React Native / Expo artifacts (should not exist in Swift projects)
node_modules/
.expo/
.bundle/
android/
ios/
babel.config.js
metro.config.js
package.json
package-lock.json
tsconfig.json
app.json

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.swp
*~

# Rork
.rork/
.env
# Locally injected secret overlay. The empty stub is force-tracked so clones
# compile; `git add .` will not stage a Rork-injected token over it.
Config.swift
